---
jobs:
  update-pkgbuild:
    if: '!github.event.release.draft && !github.event.release.prerelease'
    runs-on: ubuntu-latest
    steps:
      - name: Extract version
        id: version
        env:
          TAG_NAME: ${{ github.event.release.tag_name }}
        run: |
          TAG="$TAG_NAME"
          VERSION="${TAG#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
      - name: Checkout MINGW-packages repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
          repository: msys2/MINGW-packages
          token: ${{ secrets.MSYS2_TOKEN }}
      - name: Configure git
        run: |
          git config user.name "Andrew Udvare"
          git config user.email "audvare@gmail.com"
      - name: Update PKGBUILD
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          git remote add upstream https://github.com/msys2/MINGW-packages.git
          git fetch upstream
          git checkout master
          git reset --hard upstream/master
          cd mingw-w64-winprefs
          sed -i "s/^pkgver=.*/pkgver=${VERSION}/" PKGBUILD
          sed -i "s/^pkgrel=.*/pkgrel=1/" PKGBUILD
          NEW_SUM=$(curl -L "https://github.com/Tatsh/winprefs/archive/refs/tags/v${VERSION}.tar.gz" | sha256sum | awk '{print $1}')
          sed -i "s/^sha256sums=.*/sha256sums=('${NEW_SUM}')/" PKGBUILD
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1
        with:
          base: master
          branch: winprefs-${{ steps.version.outputs.version }}
          commit-message: 'mingw-w64-winprefs: update to ${{ steps.version.outputs.version }}'
          title: 'mingw-w64-winprefs: update to ${{ steps.version.outputs.version }}'
          token: ${{ secrets.MSYS2_TOKEN }}
          body: |
            This PR was automatically created from the [winprefs ${{ steps.version.outputs.tag }} release](${{ github.event.release.html_url }}).
          push-to-fork: Tatsh/MINGW-packages
name: Update MSYS2 PKGBUILD
'on':
  release:
    types:
      - 'published'
permissions:
  contents: 'read'
